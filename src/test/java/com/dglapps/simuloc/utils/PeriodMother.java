package com.dglapps.simuloc.utils;

import com.dglapps.simuloc.domain.trip.Period;

public class PeriodMother {
    public static Period EVERY_SECOND = Period.ofSeconds(1);
    public static Period EVERY_FIVE_SECOND = Period.ofSeconds(5);
}
